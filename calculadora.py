#! /usr/bin/python3.5

import sys

if len(sys.argv) != 4:
	sys.exit("Usage: calc.py operacion operando1 operando2")

_, operation, firstArg, secondArg = sys.argv

firstArg = float(firstArg)
secondArg = float(secondArg)






# distinto orden de argumentos
# excepcion de argumentos, operacion no entendida (en el else), division entre cero 	
# menor numero posible de lineas dentro de un try
